import 'package:flutter/material.dart';

void main() {
  //This is the main entry point for a flutter app
  runApp(
    //This inflates a widget and attaches it to the screen
    Center(
      child: Text('Hello World',
      textDirection:TextDirection.ltr,
      ),
      ),
  );
}
